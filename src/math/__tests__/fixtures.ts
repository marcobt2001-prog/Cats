import type { VisualNode, VisualEdge } from '../fromDiagram.js';
import { DEFAULT_NODES as NODES, DEFAULT_EDGES as EDGES } from '../../defaults.js';

// The editor's startup diagram, typed for the tests.
export const DEFAULT_NODES: VisualNode[] = NODES;
export const DEFAULT_EDGES: VisualEdge[] = EDGES;
